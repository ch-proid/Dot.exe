# Dot.exe

The actual game project lives under `Game/`.

- `Game/` — Dot.exe source, design documents, references, localization, tests and assets
- `.agents/` — Luna Chat Coder integration only

For game work, start with `Game/README.md` and `Game/Docs/current/CORE_GAME_RULES.md`. Historical reviews are kept separately under `Game/Docs/history/`.
